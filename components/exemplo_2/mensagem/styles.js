import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: 'orange' ,
    },
    viewmensagem: {
        borderWidth: 2,
        borderColor: 'blue' ,
        borderRadius: 20,
        marginBottom: 10,
    },

});   
export default styles;
